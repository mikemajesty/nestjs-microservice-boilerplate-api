apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nestjs-boilerplate-dev-app
  namespace: nestjs-boilerplate-dev-workload
spec:
  parentRefs:
    - name: private-origin-gateway
      namespace: envoy-gateway-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: nestjs-boilerplate-dev-app
          port: 5000
